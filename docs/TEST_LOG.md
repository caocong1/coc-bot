# 测试日志 & 需求追踪

> 测试进行中随时更新。问题和新需求记录在下方对应表格，优先级由 KP 判断。

---

## 一、测试进度

### 第一阶段：启动 & 基础验证

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 1 | `bun run src/server/index.ts` | WebSocket 已连接 + HTTP server listening | ✅ | 重复启动自动 kill 旧实例（PID 文件机制） |
| 2 | `.help` | 返回所有命令列表（含 .room、.mod，不含 .campaign） | ⬜ | |
| 3 | `.r 1d100` / `.r 3d6*5` / `.r 2d6+6` | 正常骰点输出 | ✅ | |

### 补充验证：2026-03-15（模组资产层）

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| B1 | `bun x tsc -p c:\\Users\\sorawatcher\\workspace\\coc-bot\\tsconfig.json --noEmit` | 后端与共享类型通过编译 | ✅ | 覆盖 `SessionState` / `ContextBuilder` / `KPPipeline` / `AdminRoutes` / `web/src/api.ts` |
| B2 | 在 `web/` 目录执行 `bun run build` | 前端构建成功 | ✅ | 验证新增模组资产 API 类型未破坏现有页面构建 |
| B3 | 检查 DB schema | 存在 `module_entities` / `module_items` / `module_rule_packs` | ✅ | 由 `Database.ts` 迁移创建 |
| B4 | 检查导入链路 | 模组导入后可触发元数据 / 资产 / 规则包三路提取 | ✅ | 代码路径已接入，待真实模组数据联调 |
| B5 | 检查运行时事件 | `register_entity` / `register_item` / `item_change` 已进入 `kp_events` 类型集 | ✅ | 通过类型与实现检查 |

### 补充验证：2026-03-15（开场导演与推进导演）

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| D1 | `bun x tsc -p c:\\\\Users\\\\sorawatcher\\\\workspace\\\\coc-bot\\\\tsconfig.json --noEmit` | 新增房间关系、导演偏好、OpeningDirector、SessionDirector 类型与调用全部通过编译 | ✅ | 覆盖 `CampaignHandler` / `SessionState` / `ContextBuilder` / `PlayerRoutes` / `RoomCommand` |
| D2 | 在 `web/` 目录执行 `bun run build` | 玩家端房间详情页的新关系/偏好编辑区可正常构建 | ✅ | 验证 `web/src/api.ts` 与 `RoomDetail.tsx` 的新增类型和调用 |
| D3 | 检查 DB schema | 存在 `campaign_room_relationships` 和 `campaign_rooms.director_prefs_json` | ✅ | 由 `Database.ts` 迁移创建 |
| D4 | 检查开场事件类型 | `opening_plan` / `director_marker` / `director_seed_resolved` / `director_cue` 已加入 `kp_events` 类型集 | ✅ | 通过 `SessionState` 类型与回放逻辑检查 |
| D5 | 检查开场生成链路 | `OpeningDirector` 采用 skeleton + beat 文本两步生成，beat 文本并行执行且单 beat 可独立 fallback | ✅ | 代码路径已接入 `CampaignHandler.startSession()` |

### 补充验证：2026-03-15（模组简介防剧透）

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| M1 | `bun x tsc -p c:\\Users\\sorawatcher\\workspace\\coc-bot\\tsconfig.json --noEmit` | 后端与共享摘要工具通过编译 | ✅ | 覆盖 `AdminRoutes` / `PlayerRoutes` / `ModCommand` / `src/shared/scenario/moduleDescription.ts` |
| M2 | 检查导入元数据 prompt | 自动简介只允许写单句开场钩子，只保留最初邀请、怪事或调查动机 | ✅ | 禁止提及真正身份、怪物/实体、仪式、时间循环、终局等 |
| M3 | 检查玩家端展示链路 | `/player/modules` 与 `.mod` 对现有简介做单句防剧透收口 | ✅ | 当前已导入模组无需重导即可看到更克制的简介 |

### 补充验证：2026-03-15（参考资料脏行过滤）

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| RF1 | `bun x tsc -p c:\\\\\\\\Users\\\\\\\\sorawatcher\\\\\\\\workspace\\\\\\\\coc-bot\\\\\\\\tsconfig.json --noEmit` | 玩家端参考资料接口与 Excel 抽取脚本新增清洗逻辑后仍可通过编译 | ✅ | 覆盖 `PlayerRoutes` / `scripts/extract-excel-data.ts` / `src/shared/reference/referenceDataSanitizer.ts` |
| RF2 | 请求 `/player/reference/armor` | 防具表不再返回 Excel 底部“术语解释”说明行 | ✅ | 接口层会自动过滤旧 JSON 中的脏行，无需重导 |
| RF3 | 请求 `/player/reference/vehicles` | 载具表不再返回 Excel 底部术语解释与追逐轮说明行 | ✅ | 抽取脚本也同步补上过滤，后续重建 JSON 不会再写入脏行 |

### 补充验证：2026-03-15（玩家端操作手册）

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| MNL1 | 检查玩家端 `Manual.tsx` 的房间指令区 | `.room pc` 与“到房间详情页配置人物关系”的说明在 Web 操作手册中可见 | ✅ | 与当前房间关系/绑卡实际流程一致 |

### 补充验证：2026-03-15（角色卡空白 Excel 下载）

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| XL1 | `bun x tsc -p c:\\\\\\\\Users\\\\\\\\sorawatcher\\\\\\\\workspace\\\\\\\\coc-bot\\\\\\\\tsconfig.json --noEmit` | 新增玩家端模板下载接口和角色卡页下载按钮后仍可通过编译 | ✅ | 覆盖 `PlayerRoutes` / `web/src/api.ts` / `web/src/pages/player/Characters.tsx` |
| XL2 | 玩家端“我的角色卡”页点击“空白卡下载” | 通过带鉴权的玩家 API 直接下载 `[充实车卡版本]空白卡.xlsx` | ✅ | 后端从仓库根目录读取模板文件，浏览器端用 blob 触发下载 |

### 补充验证：2026-03-15（筛卡提示 UI）

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| U1 | `bun x tsc -p c:\\\\\\\\\\\\\\\\Users\\\\\\\\\\\\\\\\sorawatcher\\\\\\\\\\\\\\\\workspace\\\\\\\\\\\\\\\\coc-bot\\\\\\\\\\\\\\\\tsconfig.json --noEmit` | 新增模组编辑、创建房间、房间详情提示文案与样式不破坏类型 | ✅ | 覆盖 `ScenarioManager` / `Rooms` / `RoomDetail` |
| U2 | 在 `web/` 目录执行 `bun run build` | 前端构建成功，筛卡提示卡片与说明区域可正常打包 | ✅ | 主要验证样式类名和条件渲染 |
| U3 | 房间详情角色卡列表 | 每张卡可直接显示“符合要求 / 不符合”及原因标签 | ✅ | 依赖 `CharacterSummary` 新增 `primaryAttributeTotal` 与 `era` 摘要字段 |
| U4 | 房间详情角色卡列表操作区 | 不符合时仍可直接点击“编辑角色卡”修改后返回重选 | ✅ | 编辑入口复用现有 `/player?edit=:id` 路由 |
| U5 | 房间详情消息历史 | 消息历史改为独立 tab，显示区域显著扩大，进入/刷新时自动定位到最新消息 | ✅ | 覆盖 `RoomDetail.tsx` 的 tab 切换与 `RoomMessages` 自动滚动逻辑 |
| U6 | 房间详情消息气泡样式 | KP 与玩家消息以不同对齐和底色的气泡显示，长消息仍保持自动换行与可读性 | ✅ | 仅调整前端展示，不影响消息数据结构 |
| U7 | 房间详情高级样式 | tab 改为分段切换器，消息增加头像圆标、系统消息独立样式与更强层次 | ✅ | 继续只影响展示层，不改接口结构 |
| U8 | 房间详情概览样式 | 页头改为概览面板，成员列表升级为更强卡片化展示，加入房间按钮移至页头快捷操作区 | ✅ | 同时修正“当前用户是否已加入房间”的判断逻辑 |
| U9 | 房间信息页区块统一 | 成员、选卡、关系等区块统一为相同面板语言与标题说明，减少页面割裂感 | ✅ | 纯前端样式与布局优化 |
| U10 | 成员列表合规提示 | 左侧成员卡片直接显示当前角色卡是否符合房间职业/总点要求，并展示需调整原因 | ✅ | 与右侧选卡列表保持一致的即时反馈 |
| U11 | 消息历史时间分隔 | 长记录按日期插入分隔条，并在大时间跨度处显示间隔提示，阅读更容易定位阶段变化 | ✅ | 仅调整前端展示逻辑，不影响消息接口 |
| U12 | 消息重点高亮 | 对检定结果、线索推进、时间推进等常见消息自动显示类型徽标，提升长记录扫读效率 | ✅ | 基于消息文本启发式判断，只影响展示层 |
| U13 | 成员卡摘要增强 | 成员卡新增职业/主属性摘要，并用更明显的卡片状态区分已准备、需调整和未选卡 | ✅ | 不新增接口，仅利用已有房间成员数据派生展示 |
| U14 | 当前绑定角色卡摘要卡 | 房间详情会单独显示玩家当前绑定角色卡、是否符合房间要求及快捷编辑/取消绑定入口 | ✅ | 复用已有角色卡摘要数据，不新增后端接口 |
| U15 | 重点消息标题条 | 检定结果、线索推进、时间推进消息增加更强的标题条和状态标签，长记录可快速定位重点内容 | ✅ | 继续基于前端启发式分类，不影响消息存储 |
| U16 | 检定消息结果卡 | 检定和理智类消息按结果头、明细与警告拆层显示，长消息阅读结构更清晰 | ✅ | 继续只调整前端展示；基于消息文本轻解析 |

### 补充验证：2026-03-15（房间即跑团）

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| R1 | `bun x tsc -p c:\\\\\\\\Users\\\\\\\\sorawatcher\\\\\\\\workspace\\\\\\\\coc-bot\\\\\\\\tsconfig.json --noEmit` | 房间详情共享化、管理端房间详情重构、旧 campaign 跳转链路通过编译 | ✅ | 覆盖 `PlayerRoutes` / `AdminRoutes` / `PlayerApp` / `RoomManager` / `RoomDetailShared` |
| R2 | 在 `web/` 目录执行 `bun run build` | 玩家端与管理端房间详情页、旧 campaign 跳转容器成功构建 | ✅ | 验证房间主体组件和 admin-only 管理区未破坏构建 |
| R3 | 玩家端导航检查 | 只保留“房间”，不再出现独立“我的团”导航 | ✅ | 旧入口仅保留跳转兼容 |
| R4 | 玩家端房间详情检查 | 消息历史、游戏时间、运行状态均可直接在房间详情查看 | ✅ | 不再需要第二个“团记录”对象 |
| R5 | 管理端房间详情检查 | 与玩家端共享主体内容，仅多出“管理区”承载导演偏好与运行时控制 | ✅ | 维持同一信息结构 |
| R6 | `.mod` / 玩家模组列表过滤 | 有文档仍处于 `pending` 导入状态的模组不会出现在玩家可见列表中 | ✅ | 管理端模组管理仍显示全部状态 |
| R7 | DashScope 自动配图 | `qwen-image-2.0-pro` 使用同步图像生成接口后，DOCX 导入自动配图不再因错误接口返回 `url error` | ✅ | 本地代码链路已修正，待真实百炼环境回归 |
| R8 | 模组场景图片聚合 | 管理端模组详情会同时显示上传图片、文档提取图片和文档补图；旧导入可经 manifest 回溯显示，新导入直接绑定到 module file id | ✅ | 覆盖 `import-pdfs.ts` / `AdminRoutes` / `ScenarioManager` |
| R9 | 模组删除级联清理 | 删除模组或删除模组文档时，会同时清理 `manifest` 条目、raw/chunks/keeper chunks 和相关图片库文件，不再遗留导入孤儿文件 | ✅ | 覆盖 `AdminRoutes.deleteModule` / `deleteModuleFile` / `deleteKnowledgeEntry` / `ImageLibrary` |
| R10 | 管理端模组图片路径与鉴权 | 模组详情图片聚合不再依赖当前工作目录，`<img>` 请求会自动附带 admin secret，独立页和列表页都能正确显示图片 | ✅ | 覆盖 `AdminRoutes` / `ImageLibrary` / `web/src/api.ts` / `ScenarioManager` |
| R11 | 管理端模组详情独立页 | `/admin/scenarios/:id` 可完整展示模组文档、场景图片、实体、物品和规则包，列表页可直接跳转 | ✅ | 覆盖 `AdminApp` / `ScenarioDetail` / `ScenarioManager` |
| R12 | 管理端模组列表收口 | 模组列表不再保留展开详情，查看与编辑统一进入独立详情页，避免两套详情交互并存 | ✅ | 列表页仅显示概览卡；详情页保留编辑能力 |
| R13 | 文档补图手动重生 | 文档自动补出的 AI 图片可在模组详情中直接“重新生成”，且通过 `createdAt` 版本参数绕开浏览器旧缓存 | ✅ | 覆盖 `AdminRoutes` / `web/src/api.ts` / `ScenarioManager` |
| R14 | 模组图片放大预览 | 管理端模组详情中的场景图片支持点击放大查看，预览层可显示来源文档与说明并支持 Esc 关闭 | ✅ | 覆盖 `ScenarioManager` / `ScenarioDetail` |
| R15 | 玩家 token 失效跳转 | 玩家端带鉴权请求收到 401 后会清理 `player_token` 并直接回首页，不再停留在失效页面 | ✅ | 覆盖 `web/src/api.ts`，含普通 JSON 请求与 Excel 导入 |
| R16 | 场景图片卡文案显示 | 管理端模组详情中的场景图片卡支持多行标题、更宽网格与分层说明，标题和介绍不再被过度截断 | ✅ | 覆盖 `ScenarioManager` 纯展示层样式 |
| R17 | 模组图片导入后即时可见 | `ImageLibrary` 读取会自动刷新磁盘状态，文档导入或自动补图完成后无需重启后端即可在模组详情看到新图片 | ✅ | 覆盖 `ImageLibrary` 读写一致性 |
| R18 | `.room pc` 自动绑定房间 | 当前群若只有一个未绑定群号的等待房间，`.room pc <角色卡名>` 会自动识别并绑定到当前群；也支持显式写 `.room pc <房间ID> <角色卡名>` | ✅ | 覆盖 `RoomCommand` 的房间查找与群绑定逻辑 |
| R19 | 角色卡编辑回填 | 打开 `/player?edit=:id` 时会加载原始角色卡 payload，并回填职业、属性、技能分配、装备、背景、法术与经历等主要表单区块 | ✅ | 覆盖 `PlayerRoutes` / `web/src/api.ts` / `CharacterForm` |
| R20 | 房间详情无效操作移除 | 玩家端房间详情不再显示“开始审卡”和“复制邀请指令”按钮，避免误导用户在该页走无效流程 | ✅ | 覆盖 `RoomDetail.tsx` / `web/src/api.ts` |
| R21 | 导演策略内收 | 玩家端和管理端都不再展示房间级导演偏好；`OpeningDirector` 改为使用系统内部全局默认策略，`preferredStartStyle=auto`，`expansionLevel=high` | ✅ | 覆盖 `StoryDirector` / `CampaignHandler` / `OpeningDirector` / `AdminRoutes` / `RoomManager` / `web/src/api.ts` |

### 第二阶段：车卡（Web 表单）

> 正式流程通过 Web 完成，提交后自动同步 bot，玩家无需发任何 QQ 命令。

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 4 | **私聊机器人**发 `.web login` → 打开链接 → 「我的角色卡」→「新建角色卡」 | 进入两步车卡向导 | ⬜ | 群内发 `.web login` 会提示去私聊 |
| 5 | Step 1：点「🎲 掷骰（4组）」 | 生成 4 组属性对比表，含总点 | ⬜ | |
| 6 | 点选一组属性 → 「确认并下一步」 | 属性锁定，进入 Step 2 | ⬜ | |
| 7 | Step 2：填姓名/年龄/职业/时代等 | 职业技能点实时计算，核心技能高亮 | ⬜ | |
| 8 | 分配职业点 + 兴趣点到技能栏 | 剩余点数实时更新，超额显示红色 | ⬜ | |
| 9 | 填写背景故事，点「💾 保存角色卡」 | 角色卡保存成功，跳转回角色列表 | ⬜ | |
| 10 | 在 QQ 群发 `.pc show` | bot 能读取到刚才 Web 提交的属性+技能 | ⬜ | 验证自动同步 |

> 备用/辅助指令（非主流程，可单独验证）

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| A1 | `.coc 3` | 返回 3 组属性，含总点 | ✅ | QQ 内快速骰卡用 |
| A2 | `.pc new 爱丽丝·韦伯` / `.pc tag` | 创建/切换角色卡 | ✅ | |
| A3 | `.st 力量:65 体质:60 ...` | 属性录入成功 | ✅ | |
| A4 | `.st export` | 导出完整 .st 命令 | ✅ | |

### 第三阶段：AI KP 跑团（含技能检定）

> 技能检定（`.ra`、`.sc`、`.en`）是跑团中途由 KP 请求或玩家触发的操作，必须在跑团启动后测试。

#### 3.0 创建房间 & 邀请入团

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 11 | 群内发 `.mod list` | 列出可用模组（ID + 名称 + 简介摘要） | ⬜ | 若无模组显示"暂无" |
| 12 | 群内发 `.room create 今晚的团 [模组ID]` | 返回房间名称 + 房间 ID | ⬜ | 模组 ID 可省略 |
| 13 | 其他玩家发 `.room join <房间ID>` | 机器人**私发**带 token 的 Web 车卡链接，群内公告"链接已私发" | ⬜ | |
| 14 | 玩家打开链接 → 进入「房间」→ 选择 PC | PC 显示在成员列表；若不符合模组约束显示 ⚠️ | ⬜ | |

#### 3.1 开团

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 15 | 创建者在目标群发 `.room start <房间ID>` | AI KP 发出三段式开场白（场景描述 + 气氛铺垫 + 行动提示） | ⬜ | 开场白内容应符合模组世界观 |
| 16 | 发言「我四处打量这个房间…」 | AI KP 叙事回复，描述环境细节 | ⬜ | |

#### 3.2 技能检定（跑团中）

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 17 | `.ra 侦查`（KP 叙事后触发） | 读角色卡技能值，显示骰点+成功等级，AI KP 基于结果接话 | ⬜ | 先有 KP 叙事再检定 |
| 18 | `.ra 图书馆使用 65` | 手动指定值检定，结果正常 | ⬜ | |
| 19 | `.ra 困难侦查` / `.ra 极难心理学` | 困难/极难修正阈值正确 | ⬜ | |
| 20 | `.ra b2 侦查` / `.ra p1 侦查` | 奖励/惩罚骰显示并计算正确 | ⬜ | |
| 21 | `.ra 3# 侦查` | 连续三次检定结果 | ⬜ | |
| 22 | `.sc 1/1d6`（KP 触发 SAN 检定） | SAN 检定骰点 + 自动扣减当前 SAN，AI KP 描述心理创伤 | ⬜ | |

#### 3.3 多频道 / 私密信息

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 23 | 公开团正常开场 | 群内不出现 `【镜头：...】`，也不出现“当前开场频道分配已建立” | ⬜ | |
| 24 | 公开团中 AI 写出“只有你注意到……” | 内容仍在群里公开推进，不触发私聊 | ⬜ | |
| 25 | 公开团中 AI 误输出 `[PRIVATE_TO:调查员]...[/PRIVATE_TO]` | 内容回落成公开叙事，不丢消息，后端有 warning | ⬜ | |
| 26 | 手动 `.scene join 图书馆` / `.scene focus 图书馆` | 只有显式手动分线后，`.scene list` 才体现多频道 | ⬜ | |
| 27 | 秘密团模组中 AI KP 回复带 `[PRIVATE_TO:调查员]...[/PRIVATE_TO]` | 指定玩家收到私聊，群里不泄露内容 | ⬜ | |
| 23 | `.en 侦查`（本次检定成功后） | 掷成长骰，成功则技能值提升并更新角色卡 | ⬜ | |

#### 3.3 暂停 & 恢复

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 24 | `.room pause` | 暂停成功，保存当前场景状态 | ⬜ | |
| 25 | `.room resume` | AI KP 两段式回顾（上次结束位置 + 继续叙事） | ⬜ | |

#### 3.4 结束团

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 26 | `.room stop` | 结束 session，AI KP 收尾总结 | ⬜ | |

### 第四阶段：模组管理（Web 管理端）

> 管理员通过 Web 创建模组实体、上传资料文件和图片；玩家端可浏览模组信息并在创建房间时选择。

#### 4.1 创建与编辑模组

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 27 | 管理员登录 → 进入「模组管理」 | 显示模组列表（空） | ⬜ | |
| 28 | 点「＋ 新建模组」，填写名称/简介/时代/职业限制/主属性总点要求 | 创建成功，模组卡片出现在列表 | ⬜ | 职业限制应从标准职业列表多选；总点默认 460，可留空 |
| 29 | 点模组卡片展开，点「编辑」修改简介 | 修改保存成功，内容更新 | ⬜ | |

#### 4.2 上传文档

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 30 | 模组展开后，在「文档」区上传 PDF 文件，填写标签和说明 | 上传成功，状态自动轮询「⟳ 导入中」→「✅ 完成」 | ⬜ | 状态每 2s 自动刷新 |
| 31 | 上传 DOCX 文件 | 自动提取文字，建索引，状态同上 | ⬜ | mammoth 解析 |
| 32 | 点「🗑 删除」某文档 | 文件从列表消失，知识索引同步清理 | ⬜ | |

#### 4.3 上传与生成图片

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 33 | 模组展开后，在「图片」区上传图片（地图/道具图等），填写标签说明 | 图片缩略图显示，标签可见 | ⬜ | |
| 34 | 点「🤖 AI 生成图片」，填写描述后点生成 | 调用图像 API，生成后显示在图片区 | ⬜ | |

#### 4.4 玩家端 & QQ 端浏览模组

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 35 | 群内发 `.mod list` | 列出已创建模组，含 ID、名称、简介摘要 | ⬜ | |
| 36 | 群内发 `.mod info <模组ID>` | 显示模组详情（职业/属性约束）和创建房间示例命令 | ⬜ | |
| 37 | 玩家端侧边栏点「模组列表」 | 列出所有已创建模组，含时代标签和约束信息 | ⬜ | |
| 38 | 创建房间时打开模组下拉框 | 列出所有模组，选中后显示约束预览（职业/总点要求） | ⬜ | |

#### 4.5 跑团与模组联动

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 39 | 从已关联模组的房间触发 `.room start` | AI KP 开场白符合模组世界观（文档内容已索引） | ⬜ | |
| 40 | 发言触发 KP | AI 叙事内容与模组场景匹配 | ⬜ | |

### 第五阶段：KP Pipeline 健壮性

> 验证并发控制、超时处理、成员过滤、KP 模板系统。

#### 5.1 并发控制

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 50 | 跑团中快速连发 2-3 条消息 | 只出一次 KP 回复（合并内容），不出多个"正在思考" | ⬜ | drain loop 合并 |
| 51 | AI 思考中第二条消息立即返回 | 无"思考中"提示、无 KP 回复（排队中） | ⬜ | `queued: true` |

#### 5.2 超时与错误

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 52 | 模拟 API 不可用（如断网） | 60s 后玩家看到"⚠️ KP 暂时无法回应，请稍后再试" | ⬜ | 自动重试一次 |
| 53 | `.ra`（无参数） | 显示格式错误，**不触发** AI 思考 | ⬜ | `error: true` 标记 |
| 54 | `.sc`（无参数） | 显示格式错误，**不触发** AI 思考 | ⬜ | 同上 |

#### 5.3 成员过滤

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 55 | 非房间成员在跑团群发言 | KP 完全不回应 | ⬜ | `isSessionMember()` |
| 56 | 非房间成员在跑团群发 `.ra 侦查` | 骰子正常回复，但 KP 不接话 | ⬜ | 骰子命令本身不限制 |
| 57 | 非房间成员在跑团群发 `.kp` | KP 不回应 | ⬜ | `handleForceKP` 过滤 |
| 58 | 无关联房间的独立 session，任何人发言 | KP 正常回应（不限制） | ⬜ | 无 room 时不过滤 |

#### 5.4 KP 模板系统

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 59 | 管理端 → KP Studio → 查看内置模板 | 显示 5 个内置模板（经典/新手友好/硬核/演绎/宇宙恐怖），只读 | ⬜ | |
| 60 | 点「+ 新建」→ 调整五维滑块 → 填自定义设定语 → 保存 | 自定义模板出现在列表 | ⬜ | |
| 61 | 编辑/删除自定义模板 | 编辑保存成功，删除后消失 | ⬜ | |
| 62 | 房间 KP 设定选择自定义模板后开团 | KP 风格符合模板五维设定 | ⬜ | 行为描述注入验证 |

#### 5.5 PC 名称显示

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 63 | 跑团中玩家发言，查看房间详情消息记录 | 消息记录显示 PC 角色名而非 QQ 昵称 | ⬜ | `resolvePcName()` |

### 第六阶段：跑团房间完整 QQ 流程

> 全程通过 QQ 命令 + Web 车卡完成，验证新的 `.room` 指令体系。

| # | 操作 | 期望 | 结果 | 备注 |
|---|------|------|------|------|
| 41 | 群内 `.room create 夜半凶杀案` | 创建成功，返回 8 位房间 ID | ⬜ | 无需填群号 |
| 42 | 同群其他玩家发 `.room join <ID>` | 机器人私聊发 Web 链接（含 token），群内公告 | ⬜ | |
| 43 | 玩家点链接 → 进入「跑团房间」→ 选择角色卡 | PC 显示在成员列表 | ⬜ | |
| 44 | 创建者发 `.room list` | 列出自己创建/参与的活跃房间 | ⬜ | |
| 45 | 创建者发 `.room info <ID>` | 显示成员列表和各自 PC | ⬜ | |
| 46 | 创建者在目标群发 `.room start <ID>` | QQ 群收到 AI KP 开场白 | ⬜ | 可与创建时不是同一个群 |
| 47 | 发 `.room pause` → `.room resume` | 暂停/恢复正常，AI 生成回顾摘要 | ⬜ | |
| 48 | 发 `.room stop` | 结束 session，收尾总结 | ⬜ | |
| 49 | 管理员在后台「房间管理」删除进行中的房间 | 出现二次确认弹框，确认后删除成功 | ⬜ | |

> 结果标记：✅ 通过 / ❌ 失败 / ⚠️ 部分通过 / ⬜ 未测

---

## 二、问题记录

| # | 发现步骤 | 问题描述 | 严重程度 | 状态 | 修复说明 |
|---|---------|---------|---------|------|---------|
| — | — | — | — | — | — |

> 严重程度：🔴 阻断 / 🟡 影响体验 / 🟢 轻微

---

## 三、新需求记录

| # | 来源 | 需求描述 | 优先级 | 处理时机 | 状态 |
|---|------|---------|--------|---------|------|
| 1 | 设计 | **Web 车卡表单**：分两步——①掷属性（多组选一）②分配技能点（职业点+兴趣点实时预算）；提交后自动同步 bot，玩家无需发任何命令 | 高 | 📅 测完后改 | ✅ 已实现 |
| 2 | 设计 | **跑团房间系统**：房间不绑定固定群，任意 QQ 群可接着跑；`.room` 指令体系管理完整生命周期 | 高 | 📅 测完后改 | ✅ 已实现 |
| 3 | 设计 | **房间加入方式**：`.room join <id>` 直接加入并私发 Web 链接，无需 `.web room` 中转 | 高 | 📅 测完后改 | ✅ 已实现 |
| 4 | 设计 | **权限**：仅管理员 / 普通成员两级。管理员可删任意房间，普通成员可建团、提交 PC、触发开始 | 中 | 📅 测完后改 | ✅ 已实现 |
| 5 | 设计 | **PC 验证**：软验证，不符合模组要求显示 ⚠️ 但允许强行开始 | 中 | 📅 测完后改 | ✅ 已实现 |
| 6 | 设计 | **模组约束配置**：创建/编辑房间时可填写约束（年代/职业限制/主属性总点要求），房间详情展示约束并检查 PC | 中 | 📅 测完后改 | ✅ 已实现 |
| 7 | 设计 | **模组管理系统**：Admin 可创建模组实体，支持多文档（PDF/DOCX/TXT）+ 图片（上传或 AI 生成），模组关联房间，KP 叙事参考模组内容 | 高 | 📅 测完后改 | ✅ 已实现 |
| 8 | 设计 | **跑团导出/导入**：全量导出（团配置+PC+消息历史+场景状态），用于备份和数据迁移 | 低 | 💡 待评估 | 📝 |

> 处理时机：🔥 测试中立即改 / 📅 测完后改 / 💡 待评估
> 状态：📝 记录 / 🚧 开发中 / ✅ 完成

---

## 四、已实现功能概览

### 本次新增（2026-03-12）

#### 跑团房间 & QQ 指令体系

- **房间不绑定群**：`campaign_rooms.group_id` 为 nullable，创建时不填群号
- **`.room` 指令**（完整生命周期）：
  - `create` — 创建房间，可绑定模组 ID
  - `join` — 加入房间，私发带 token 的 Web 车卡链接
  - `start` — 在当前群开始跑团（仅创建者）
  - `pause / resume / stop` — 暂停/恢复/结束当前群的 session
  - `list / info` — 查看我的活跃房间 / 房间详情
- **`.mod` 指令**：`list [页]` / `info <ID>` — 在 QQ 内浏览模组
- **`.web login` 私聊限定**：群内发送返回提示去私聊，避免 token 泄露

#### 模组管理系统

- **DB**：`scenario_modules` 表（id / name / description / era / allowed_occupations / min_stats）+ `scenario_module_files` 表（关联模组的文档和图片，含 import_status）
- **后端 API**（`/api/admin/modules/*`）：
  - CRUD 模组（创建/查看列表/查看详情/编辑/删除）
  - 上传文档（PDF/DOCX/TXT）→ 后台触发建索引，状态写回 DB，前端自动轮询
  - 上传图片 / AI 生成图片（存储在 `data/knowledge/images/modules/{moduleId}/`）
  - 删除文档/图片
- **玩家 API**（`/api/player/modules`）：列出所有模组（含约束，不含文件详情）
- **前端 Admin**：`ScenarioManager`，模组卡片展开显示文档列表（上传/删除/状态自动刷新）+ 图片区
- **前端 Player**：「模组列表」页；创建房间时模组下拉选择 + 约束预览

#### Web 车卡两步向导
- **Step 1**：掷出 4 组属性对比表，玩家选一组
- **Step 2**：属性只读展示，选择职业后显示职业技能点/兴趣点预算，分配技能，填背景故事
- 提交调用 `playerApi.createCharacter()` / `updateCharacter()`，自动同步 bot DB

#### 启动稳定性
- `data/bot.pid` 管理：启动时自动 kill 旧实例，退出时清理

---

## 五、测试环境

- Bot 版本：`master`
- 测试时间：2026-03-12
- NapCat 版本：—（待填）
- 测试群号：—（待填）
- AI 模型：qwen3.5-plus（KP）/ qwen3.5-flash（guardrail）

---

*最后更新：2026-03-14（新增第五阶段：并发控制 / 超时重试 / 成员过滤 / KP 模板 / PC 名称）*
